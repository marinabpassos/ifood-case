# Phase 1 Data Model: Repo Scaffold

Not applicable. This feature creates directory structure and documentation
only — it introduces no domain entities, no persisted records, and no
state transitions (see spec `Key Entities: N/A`). The first data model
(taxi trip records, silver-layer schema) belongs to a later ingestion/
contracts feature, at which point this file's counterpart in that
feature's spec directory will carry the real entity definitions.
